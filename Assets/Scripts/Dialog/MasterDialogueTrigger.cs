using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// DialogueTriggers only trigger when this is enabled
public class MasterDialogueTrigger : Singleton<MasterDialogueTrigger>
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
